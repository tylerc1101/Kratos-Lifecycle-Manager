---
- name: Placeholder - OpenSpace generate config
  hosts: all
  gather_facts: false
  tasks:
    - name: Replace this placeholder before use
      ansible.builtin.fail:
        msg: "TODO: implement OpenSpace generate config"
