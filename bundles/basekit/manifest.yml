name: basekit
version: 0.1.0
type: infrastructure

description: OpenSpace reference architecture designed for minimal footprint deployments

taskfile:
  include: Taskfile.yml
  namespace: Basekit

path:
  add: []

requires:
  commands:
    - python3
    - tar
